<% @todos.each do |todo| %>
  <div class="index_row clearfix">
    <h2 class="todo_list_title"><%= link_to todo.title, todo %>
    <p class="todo_list_sub_title"><%= todo.description %></p>
    </h2>
  </div>
<% end %>

<div class="links">
  <%= link_to "New Todo List", new_todo_path %>
</div>
